{
  "name": "@hrs-website/root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "prettier --check .",
    "build": "npm run build -w packages/app",
    "deploy": "npm run deploy -w packages/cdk",
    "prepare": "husky"
  },
  "workspaces": [
    "./packages/*"
  ],
  "devDependencies": {
    "husky": "^9.0.11",
    "prettier": "^2.3.2"
  }
}
