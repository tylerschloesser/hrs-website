{
  "name": "@hrs-website/root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "test": "prettier --check .",
    "build": "npm run build -w app",
    "deploy": "npm run deploy -w cdk"
  },
  "workspaces": [
    "./packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "prettier": "^2.3.2"
  }
}
