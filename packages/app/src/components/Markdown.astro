---
import { renderMarkdown } from '../lib/markdown'

interface Props {
  content: string
}

const { content } = Astro.props

const html = renderMarkdown(content)
---

<div class="prose-hrs" set:html={html} />
